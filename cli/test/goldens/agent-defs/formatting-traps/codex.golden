name = "formatting-traps"
description = "@edge: use [brackets] & {braces} *stars* with CJK 中文 and emoji 🚀 padded out long enough to trigger PyYAML line wrapping past eighty columns"
developer_instructions = """# Trap instructions

A line with a colon: value, and specials @ & * [ ] { } to stress escaping.

Unicode body: 中文 and 🚀 rendered without escaping in TOML.


Final paragraph before trailing blank lines.


"""
model = "gpt-5.5"
model_reasoning_effort = "high"
sandbox_mode = "read-only"
nickname_candidates = ["Trap"]
